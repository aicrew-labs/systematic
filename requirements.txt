fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
python-multipart==0.0.9
aiofiles==23.2.1
openai>=1.0.0
python-dotenv>=1.0.0
supabase>=2.0.0
python-dateutil>=2.8.2

# ETL/seed-only deps (not needed at runtime, but kept here so the same
# requirements file works for both Railway and local ETL runs).
pandas==2.2.3
openpyxl==3.1.5
xlrd==2.0.1
sqlalchemy==2.0.35
